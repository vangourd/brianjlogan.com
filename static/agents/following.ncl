# Following List
# Sites and people I follow
# AI tools can read this to discover what I'm keeping up with

{
  version = "1.0",
  following = [
    # Add sites here in the format:
    # {
    #   url = "https://example.com",
    #   added = "2025-02-07",
    #   tags = ["tech", "blog"],
    #   notes = "Optional description"
    # },
  ]
}
